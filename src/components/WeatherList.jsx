/* import React from 'react';
import WeatherCard from './WeatherCard';

const WeatherList = ({ weathers }) => (
    <div className="weather-list">
      {weathers.map((weather) => (
        <WeatherCard key={weather.ts} weather={weather} />
      ))}
    </div>
  );
      
export default WeatherList; */